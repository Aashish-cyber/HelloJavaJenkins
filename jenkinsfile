pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Compile and build your Java application
                sh 'javac Main.java'
            }
        }

        stage('Run') {
            steps {
                // Run your Java application
                sh 'java Main'
            }
        }
    }
}
:w
